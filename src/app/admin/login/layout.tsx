import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'دخول الإدارة | مركز حي الشاطئ',
}

export default function AdminLoginLayout({ children }: { children: React.ReactNode }) {
  return children
}
